const config = {
    client: 'pg',
    connection: {
        host : 'localhost',
        user : 'postgres',
        password : '1234567',
        database : 'postgres',
    }
};

module.exports = config;