const config = {
    client: 'pg',
    connection: {
        host : 'localhost',
        user : 'postgresql-curly-06855',
        password : '',
        database : 'postgresql-curly-06855'
    }
};

module.exports = config;